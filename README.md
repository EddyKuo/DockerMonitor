# DockerMonitor

> 透過跳板機（Bastion Server）監控多台主機上 Docker 容器的現代化終端介面工具

[![Python Version](https://img.shields.io/badge/python-3.8%2B-blue.svg)](https://www.python.org/downloads/)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![Textual](https://img.shields.io/badge/TUI-Textual-purple.svg)](https://textual.textualize.io/)

## 📋 目錄

- [專案概述](#專案概述)
- [專案目標與目的](#專案目標與目的)
- [核心特色](#核心特色)
- [實作方案](#實作方案)
- [技術方案](#技術方案)
- [功能項目](#功能項目)
- [系統需求](#系統需求)
- [安裝與建置](#安裝與建置)
- [配置說明](#配置說明)
- [使用說明](#使用說明)
- [專案結構](#專案結構)
- [開發指南](#開發指南)
- [疑難排解](#疑難排解)
- [版本規劃](#版本規劃)
- [授權與貢獻](#授權與貢獻)

## 專案概述

**DockerMonitor** 是一個基於 Python 的 Docker 容器監控工具，專為需要透過跳板機（Jump Host/Bastion Server）存取私有網路中多台 Docker 主機的場景設計。提供現代化的終端使用者介面（TUI）和命令列介面（CLI），讓您能夠即時監控容器狀態、資源使用情況，並進行高效的多主機管理。

### 主要亮點

- 🖥️ **現代化 TUI 介面** - 基於 Textual 框架的美觀互動式終端介面
- 🔒 **安全的 SSH 連線** - 透過跳板機安全存取私有網路主機
- ⚡ **非同步高效能** - 使用 asyncio 實現並發監控多台主機
- 🐳 **無需 Docker API** - 僅透過 Docker CLI 操作，無需開放 Docker API 埠
- 📊 **即時資源監控** - CPU、記憶體使用率即時顯示
- 🏷️ **標籤分類管理** - 使用標籤組織和篩選主機
- 🌐 **多語言支援** - 支援繁體中文與英文介面切換

## 專案目標與目的

### 解決的問題

在現代化的 DevOps 環境中，Docker 容器通常部署在私有網路的多台主機上，而這些主機只能透過跳板機存取。傳統的監控方式存在以下問題：

1. **存取複雜** - 需要多次 SSH 跳轉才能查看各主機狀態
2. **效率低下** - 逐一登入各主機執行 `docker ps` 耗時費力
3. **缺乏整合** - 無法統一檢視所有主機的容器狀態
4. **安全顧慮** - 開放 Docker API 埠存在安全風險
5. **資訊分散** - 難以快速掌握整體容器運行狀況

### 使用場景

- **生產環境監控** - 即時監控生產環境中所有 Docker 容器狀態
- **多環境管理** - 同時管理開發、測試、生產等多個環境
- **故障排查** - 快速定位異常容器並查看詳細資訊
- **資源優化** - 監控容器資源使用情況，優化資源配置
- **日常維運** - 簡化日常容器狀態檢查流程

### 設計理念

1. **安全第一** - 僅使用 SSH 金鑰認證，不儲存密碼，不開放額外埠
2. **簡單易用** - 直覺的 TUI 介面，最小化學習成本
3. **高效能** - 非同步並發操作，快速獲取多主機資訊
4. **可擴展** - 模組化設計，易於擴展新功能
5. **零侵入** - 無需在目標主機安裝任何代理程式

## 核心特色

### TUI 互動介面

- ✨ **美觀的終端介面** - 使用 Textual 框架打造的現代化 TUI
- 📋 **主機列表側邊欄** - 顯示所有主機及連線狀態
- 📊 **容器表格** - 可排序、可篩選的容器列表
- 📈 **統計面板** - 即時顯示主機與容器統計資訊
- ⌨️ **快捷鍵操作** - 豐富的鍵盤快捷鍵，提升操作效率
- 🔄 **自動重新整理** - 可設定自動重新整理間隔
- 🔍 **容器詳情檢視** - 查看容器完整資訊
- ❓ **內建說明系統** - 隨時查看操作說明

### CLI 命令列介面

- 📄 **多種輸出格式** - 支援表格、JSON、CSV 格式
- 🏷️ **標籤篩選** - 根據標籤篩選要監控的主機
- 💾 **匯出功能** - 將監控資料匯出至檔案
- 🐛 **除錯模式** - 詳細的日誌輸出協助問題排查

### 監控功能

- 🐳 **容器狀態監控** - 即時顯示容器運行狀態
- 💻 **資源使用監控** - CPU、記憶體使用率
- 🌐 **網路與磁碟 I/O** - 網路流量與磁碟讀寫統計
- 🔌 **連線狀態監控** - 主機連線狀態即時更新
- 🐋 **Docker 版本檢測** - 自動檢測 Docker 可用性與版本

### 配置管理

- 📝 **YAML 配置** - 簡潔的 YAML 格式主機配置
- 🌍 **環境變數支援** - 支援 .env 檔案配置
- 🏷️ **主機標籤系統** - 使用標籤組織主機
- 🔧 **彈性配置** - 可針對各主機獨立配置

## 實作方案

### 整體架構

```
┌─────────────────────────────────────────────────────────────┐
│                     本地端 (DockerMonitor)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   TUI 介面   │  │   CLI 介面   │  │  配置管理器   │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
│         │                 │                  │              │
│         └─────────────────┴──────────────────┘              │
│                           │                                 │
│                  ┌────────▼────────┐                        │
│                  │  Docker Monitor  │                        │
│                  │   (監控核心)      │                        │
│                  └────────┬────────┘                        │
│                           │                                 │
│                  ┌────────▼────────┐                        │
│                  │  SSH Tunnel Mgr │                        │
│                  │  (SSH 隧道管理)  │                        │
│                  └────────┬────────┘                        │
└───────────────────────────┼─────────────────────────────────┘
                            │ SSH 連線
                            ▼
              ┌─────────────────────────┐
              │   跳板機 (Jump Host)     │
              │   - SSH 認證             │
              │   - 連線轉發             │
              └────────┬────────────────┘
                       │ SSH 隧道
         ┌─────────────┼─────────────┐
         │             │             │
         ▼             ▼             ▼
    ┌────────┐    ┌────────┐    ┌────────┐
    │ 主機 1  │    │ 主機 2  │    │ 主機 N  │
    │ Docker │    │ Docker │    │ Docker │
    └────────┘    └────────┘    └────────┘
    私有網路       私有網路       私有網路
```

### 連線流程

1. **初始化階段**
   - 讀取 `config/hosts.yaml` 配置檔
   - 載入 SSH 金鑰與認證資訊
   - 初始化日誌系統

2. **建立連線**
   - 連線至跳板機（Jump Host）
   - 透過跳板機建立至各目標主機的 SSH 隧道
   - 驗證 Docker 可用性

3. **資料收集**
   - 並發執行 `docker ps` 獲取容器列表
   - 執行 `docker stats --no-stream` 獲取資源使用率
   - 解析 JSON 格式輸出

4. **資料處理**
   - 彙整各主機資料
   - 計算統計資訊
   - 更新 UI 顯示

5. **連線管理**
   - 每次重新整理後關閉連線（避免連線堆積）
   - 錯誤處理與重試機制
   - 優雅的連線清理

### 資料流向

```
配置檔 → 配置管理器 → SSH 隧道管理器 → 遠端執行器 → Docker CLI
                                                          ↓
TUI/CLI ← 資料彙整器 ← Docker 輸出解析器 ← JSON 輸出 ←┘
```

## 技術方案

### 技術棧

#### 核心框架

- **Python 3.8+** - 主要程式語言
  - 選擇理由：豐富的生態系統、優秀的非同步支援、跨平台相容性

- **Textual 0.47+** - TUI 框架
  - 選擇理由：現代化的 TUI 框架、React-like 的開發體驗、豐富的元件庫
  - 官網：https://textual.textualize.io/

- **AsyncSSH 2.14+** - 非同步 SSH 客戶端
  - 選擇理由：純 Python 實作、完整的 SSH 協定支援、優秀的非同步效能
  - 官網：https://asyncssh.readthedocs.io/

#### 輔助套件

- **Rich 13.7+** - 終端格式化
  - 用途：美化終端輸出、表格渲染、語法高亮

- **Click 8.1+** - CLI 框架
  - 用途：命令列參數解析、子命令管理

- **PyYAML 6.0+** - YAML 解析器
  - 用途：讀取配置檔

- **python-dotenv 1.0+** - 環境變數管理
  - 用途：載入 .env 檔案

#### 開發工具

- **pytest 7.4+** - 測試框架
- **pytest-asyncio 0.21+** - 非同步測試支援
- **black 23.12+** - 程式碼格式化
- **flake8 6.1+** - 程式碼檢查
- **mypy 1.7+** - 型別檢查

### 核心技術選型理由

#### 為何使用 Docker CLI 而非 Docker API？

1. **安全性** - 無需開放 Docker API 埠（通常是 2375/2376）
2. **簡單性** - 無需配置 TLS 憑證
3. **相容性** - 適用於所有 Docker 版本
4. **最小權限** - 僅需 SSH 存取權限

#### 為何使用非同步架構？

1. **效能** - 並發監控多台主機，大幅減少等待時間
2. **資源效率** - 單執行緒處理多個連線，降低資源消耗
3. **回應性** - UI 不會因網路操作而凍結
4. **可擴展性** - 輕鬆擴展至監控數十台主機

#### 為何選擇 Textual 而非其他 TUI 框架？

1. **現代化** - 使用 CSS 樣式、React-like 元件模型
2. **豐富元件** - 內建大量常用 UI 元件
3. **開發體驗** - 優秀的開發者工具與文件
4. **活躍維護** - 持續更新與改進

### 依賴套件說明

完整依賴列表請參考 [requirements.txt](requirements.txt)

```
textual>=0.47.0      # TUI 框架
rich>=13.7.0         # 終端格式化
asyncssh>=2.14.0     # SSH 客戶端
cryptography>=41.0.0 # SSH 金鑰處理
pyyaml>=6.0.1        # YAML 解析
python-dotenv>=1.0.0 # 環境變數
click>=8.1.7         # CLI 框架
```

## 功能項目

### TUI 介面功能

#### 主畫面

- **主機列表側邊欄**
  - 顯示所有配置的主機
  - 即時連線狀態指示（🟢 已連線 / 🔴 未連線）
  - 點擊主機可篩選該主機的容器
  - 顯示各主機容器數量

- **容器表格**
  - 顯示所有容器的詳細資訊
  - 欄位：名稱、映像、狀態、CPU、記憶體、網路 I/O、磁碟 I/O、主機
  - 支援多欄位排序（名稱、映像、狀態、CPU、記憶體）
  - 狀態顏色編碼（綠色=運行中、紅色=已停止）
  - 按 Enter 查看容器詳細資訊

- **統計面板**
  - 總主機數 / 已連線主機數
  - 總容器數 / 運行中容器數
  - 總 CPU 使用率
  - 總記憶體使用量

- **狀態列**
  - 顯示當前操作狀態
  - 最後更新時間
  - 快捷鍵提示

#### 容器詳情畫面

- 容器基本資訊（ID、名稱、映像）
- 運行狀態與建立時間
- 資源使用統計
- 網路設定與埠映射
- 環境變數
- 掛載點資訊

#### 說明畫面

- 快捷鍵列表
- 功能說明
- 使用技巧
- 疑難排解指引

### CLI 功能

#### status 命令

獲取所有容器的當前狀態

```bash
# 基本用法
python -m src.main status

# 篩選特定標籤
python -m src.main status --tags production,web

# JSON 格式輸出
python -m src.main status --format json

# CSV 格式輸出
python -m src.main status --format csv

# 儲存至檔案
python -m src.main status --format json --output status.json
```

#### tui 命令

啟動互動式 TUI 介面

```bash
# 基本用法
python -m src.main tui

# 篩選特定標籤
python -m src.main tui --tags production

# 自訂重新整理間隔（秒）
python -m src.main tui --interval 30

# 啟用除錯模式
python -m src.main --debug tui
```

### 快捷鍵

| 按鍵 | 功能 | 說明 |
|------|------|------|
| `h` | 說明 | 顯示說明畫面 |
| `r` | 重新整理 | 手動重新整理資料 |
| `s` | 排序 | 循環切換排序欄位 |
| `f` | 篩選 | 開啟篩選對話框（規劃中） |
| `l` | 語言 | 切換介面語言（中文/英文） |
| `q` | 離開 | 退出應用程式 |
| `Tab` | 切換焦點 | 在不同元件間切換 |
| `↑/↓` | 導覽 | 在列表中上下移動 |
| `←/→` | 切換主機 | 在主機列表中切換 |
| `Enter` | 詳情 | 查看容器詳細資訊 |
| `/` | 搜尋 | 搜尋容器（規劃中） |

## 系統需求

### 必要需求

- **Python 版本**：Python 3.8 或更高版本
- **作業系統**：
  - Windows 10/11
  - Linux (Ubuntu 18.04+, Debian 10+, CentOS 7+, 等)
  - macOS 10.14+

### SSH 需求

- **跳板機**：
  - SSH 伺服器運行中
  - 允許 SSH 金鑰認證
  - 可存取目標主機

- **目標主機**：
  - SSH 伺服器運行中
  - 已安裝 Docker
  - 使用者具有執行 Docker 命令的權限（通常需加入 docker 群組）

### 網路需求

- 本地端可連線至跳板機
- 跳板機可連線至所有目標主機
- 目標主機無需對外開放任何埠

## 安裝與建置

### 方法一：使用快速啟動腳本（推薦）

#### Windows

```powershell
# 下載或克隆專案
cd DockerMonitor

# 執行啟動腳本（會自動建立虛擬環境並安裝依賴）
.\run.bat
```

#### Linux/Mac

```bash
# 下載或克隆專案
cd DockerMonitor

# 賦予執行權限並執行
chmod +x run.sh
./run.sh
```

### 方法二：手動安裝

#### 1. 建立虛擬環境

```bash
# 建立虛擬環境
python -m venv venv

# 啟動虛擬環境
# Windows:
venv\Scripts\activate

# Linux/Mac:
source venv/bin/activate
```

#### 2. 安裝依賴套件

```bash
# 升級 pip
python -m pip install --upgrade pip

# 安裝所有依賴
pip install -r requirements.txt
```

#### 3. 驗證安裝

```bash
# 檢查版本
python -m src.main --help

# 應該會顯示使用說明
```

### 方法三：使用 setup.py 安裝

```bash
# 開發模式安裝（推薦用於開發）
pip install -e .

# 正式安裝
pip install .

# 安裝後可直接使用命令
dockermonitor --help
dockermonitor tui
dockermonitor status
```

## 配置說明

### 配置檔案結構

```
DockerMonitor/
├── .env                    # 環境變數配置（可選）
└── config/
    └── hosts.yaml          # 主機配置（必要）
```

### 建立配置檔

```bash
# Windows
copy .env.example .env
copy config\hosts.yaml.example config\hosts.yaml

# Linux/Mac
cp .env.example .env
cp config/hosts.yaml.example config/hosts.yaml
```

### hosts.yaml 配置說明

#### 基本結構

```yaml
# 跳板機配置
jump_host:
  hostname: jump.example.com    # 跳板機位址
  port: 22                      # SSH 埠（預設 22）
  username: your_username       # SSH 使用者名稱
  key_file: ~/.ssh/id_rsa      # SSH 私鑰路徑
  # password: your_password     # 密碼（不建議使用）

# 目標主機列表
target_hosts:
  - name: prod-web-01           # 主機名稱（唯一識別）
    hostname: 192.168.1.10      # 主機位址
    port: 22                    # SSH 埠
    username: docker_user       # SSH 使用者名稱
    key_file: ~/.ssh/id_rsa    # SSH 私鑰路徑
    description: "生產環境 Web 伺服器"  # 描述（可選）
    tags:                       # 標籤（可選）
      - production
      - web
    enabled: true               # 是否啟用（預設 true）

  - name: prod-db-01
    hostname: 192.168.1.20
    port: 22
    username: docker_user
    key_file: ~/.ssh/id_rsa
    description: "生產環境資料庫伺服器"
    tags:
      - production
      - database
    enabled: true

# 監控配置
monitoring:
  command_timeout: 30           # 命令執行逾時（秒）
  max_retries: 3               # 最大重試次數
  retry_delay: 5               # 重試延遲（秒）
  max_concurrent_connections: 5 # 最大並發連線數

# Docker 配置
docker:
  docker_bin: /usr/bin/docker  # Docker 執行檔路徑
```

#### 標籤系統

使用標籤組織主機，方便篩選：

```yaml
target_hosts:
  - name: web-01
    tags: [production, web, frontend]
  
  - name: api-01
    tags: [production, api, backend]
  
  - name: db-01
    tags: [production, database]
  
  - name: dev-web-01
    tags: [development, web]
```

篩選使用：

```bash
# 只監控生產環境
python -m src.main tui --tags production

# 監控所有 Web 伺服器
python -m src.main tui --tags web

# 多標籤篩選（OR 邏輯）
python -m src.main tui --tags production,database
```

### .env 環境變數配置

```bash
# 跳板機配置（可覆寫 hosts.yaml）
JUMP_HOST=jump.example.com
JUMP_USER=your_username
JUMP_KEY_PATH=~/.ssh/id_rsa

# 監控配置
MONITOR_INTERVAL=60          # 自動重新整理間隔（秒）
TIMEOUT=30                   # 命令逾時（秒）
MAX_WORKERS=5               # 最大並發數

# 輸出配置
OUTPUT_FORMAT=table         # 預設輸出格式（table/json/csv）
LOG_LEVEL=INFO             # 日誌等級（DEBUG/INFO/WARNING/ERROR）

# 語言配置
LANGUAGE=zh_TW             # 介面語言（zh_TW/en）
```

### SSH 金鑰設定

#### 產生 SSH 金鑰

```bash
# 產生新的 SSH 金鑰對
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"

# 設定正確的權限
chmod 600 ~/.ssh/id_rsa
chmod 644 ~/.ssh/id_rsa.pub
```

#### 複製公鑰至伺服器

```bash
# 複製至跳板機
ssh-copy-id -i ~/.ssh/id_rsa.pub user@jump.example.com

# 複製至目標主機（透過跳板機）
ssh-copy-id -i ~/.ssh/id_rsa.pub -o ProxyJump=user@jump.example.com user@target-host
```

#### 測試 SSH 連線

```bash
# 測試跳板機連線
ssh -i ~/.ssh/id_rsa user@jump.example.com

# 測試目標主機連線（透過跳板機）
ssh -i ~/.ssh/id_rsa -J user@jump.example.com user@target-host

# 測試 Docker 權限
ssh -i ~/.ssh/id_rsa -J user@jump.example.com user@target-host "docker ps"
```

## 使用說明

### TUI 模式（推薦）

#### 基本啟動

```bash
# 啟動 TUI 介面
python -m src.main tui
```

#### 進階選項

```bash
# 篩選特定標籤的主機
python -m src.main tui --tags production

# 設定自動重新整理間隔為 30 秒
python -m src.main tui --interval 30

# 啟用除錯模式（日誌會寫入檔案，不會顯示在畫面上）
python -m src.main --debug tui

# 組合使用
python -m src.main --debug tui --tags production,web --interval 45
```

#### TUI 操作流程

1. **啟動應用程式**
   ```bash
   python -m src.main tui
   ```

2. **檢視主機列表**
   - 左側邊欄顯示所有主機
   - 🟢 表示已連線，🔴 表示未連線
   - 使用 `↑/↓` 鍵選擇主機

3. **檢視容器列表**
   - 中央表格顯示所有容器
   - 使用 `Tab` 鍵切換焦點至容器表格
   - 使用 `↑/↓` 鍵選擇容器

4. **查看容器詳情**
   - 選擇容器後按 `Enter`
   - 顯示完整的容器資訊
   - 按 `Esc` 或 `q` 返回主畫面

5. **排序容器**
   - 按 `s` 鍵循環切換排序欄位
   - 排序欄位：名稱 → 映像 → 狀態 → CPU → 記憶體

6. **重新整理資料**
   - 按 `r` 鍵手動重新整理
   - 或等待自動重新整理（預設 60 秒）

7. **切換語言**
   - 按 `l` 鍵切換中文/英文介面

8. **查看說明**
   - 按 `h` 鍵顯示說明畫面

9. **離開程式**
   - 按 `q` 鍵離開

### CLI 模式

#### 獲取容器狀態

```bash
# 顯示所有容器（表格格式）
python -m src.main status

# JSON 格式輸出
python -m src.main status --format json

# CSV 格式輸出
python -m src.main status --format csv

# 儲存至檔案
python -m src.main status --format json --output containers.json
python -m src.main status --format csv --output containers.csv
```

#### 篩選主機

```bash
# 只顯示生產環境主機
python -m src.main status --tags production

# 只顯示 Web 伺服器
python -m src.main status --tags web

# 多標籤篩選
python -m src.main status --tags production,database
```

#### 除錯模式

```bash
# 啟用除錯日誌
python -m src.main --debug status

# 查看日誌檔案
# Windows:
type logs\dockermonitor_*.log

# Linux/Mac:
cat logs/dockermonitor_*.log
```

### 常見使用案例

#### 案例 1：快速健康檢查

```bash
# 快速檢視所有容器狀態
python -m src.main status --format table
```

#### 案例 2：監控生產環境

```bash
# 只監控生產環境，30 秒重新整理一次
python -m src.main tui --tags production --interval 30
```

#### 案例 3：匯出報告

```bash
# 匯出 JSON 格式報告供其他工具使用
python -m src.main status --format json --output report_$(date +%Y%m%d).json
```

#### 案例 4：定期監控腳本

```bash
# 建立定期執行的監控腳本
#!/bin/bash
while true; do
    python -m src.main status --format json --output /var/log/docker_status.json
    sleep 300  # 每 5 分鐘執行一次
done
```

## 專案結構

```
DockerMonitor/
├── config/                      # 配置檔案目錄
│   ├── hosts.yaml              # 主機配置
│   └── hosts.yaml.example      # 配置範例
│
├── locales/                     # 多語言翻譯檔
│   ├── en/                     # 英文
│   │   └── messages.yaml
│   └── zh_TW/                  # 繁體中文
│       └── messages.yaml
│
├── logs/                        # 日誌檔案目錄（自動建立）
│   └── dockermonitor_*.log
│
├── src/                         # 原始碼目錄
│   ├── __init__.py
│   ├── main.py                 # 主程式進入點
│   │
│   ├── aggregator/             # 資料彙整模組
│   │   ├── __init__.py
│   │   └── data.py            # 資料彙整與格式化
│   │
│   ├── docker/                 # Docker 監控模組
│   │   ├── __init__.py
│   │   ├── monitor.py         # Docker 監控核心
│   │   └── parser.py          # Docker 輸出解析器
│   │
│   ├── i18n/                   # 國際化模組
│   │   ├── __init__.py
│   │   └── translator.py      # 翻譯器
│   │
│   ├── ssh/                    # SSH 連線模組
│   │   ├── __init__.py
│   │   ├── executor.py        # 遠端命令執行器
│   │   └── tunnel.py          # SSH 隧道管理器
│   │
│   ├── tui/                    # TUI 介面模組
│   │   ├── __init__.py
│   │   ├── app.py             # TUI 應用程式主體
│   │   ├── styles.css         # TUI 樣式表
│   │   │
│   │   ├── screens/           # TUI 畫面
│   │   │   ├── __init__.py
│   │   │   ├── main_screen.py    # 主畫面
│   │   │   ├── detail_screen.py  # 詳情畫面
│   │   │   └── help_screen.py    # 說明畫面
│   │   │
│   │   └── widgets/           # TUI 元件
│   │       ├── __init__.py
│   │       ├── container_table.py  # 容器表格
│   │       ├── host_list.py        # 主機列表
│   │       ├── stats_panel.py      # 統計面板
│   │       └── status_bar.py       # 狀態列
│   │
│   └── utils/                  # 工具模組
│       ├── __init__.py
│       ├── config.py          # 配置管理器
│       └── logger.py          # 日誌系統
│
├── tests/                       # 測試目錄
│   ├── __init__.py
│   ├── test_config.py
│   ├── test_logger.py
│   ├── test_parser.py
│   └── test_aggregator.py
│
├── .env.example                 # 環境變數範例
├── .gitignore                   # Git 忽略檔案
├── CHANGELOG.md                 # 版本更新記錄
├── CONTRIBUTING.md              # 貢獻指南
├── LICENSE                      # 授權條款
├── Makefile                     # Make 指令
├── pytest.ini                   # pytest 配置
├── README.md                    # 本檔案
├── requirements.txt             # Python 依賴
├── run.bat                      # Windows 啟動腳本
├── run.sh                       # Linux/Mac 啟動腳本
└── setup.py                     # 安裝配置
```

### 模組職責說明

- **main.py** - CLI 命令定義與應用程式進入點
- **aggregator/** - 負責彙整多主機資料並格式化輸出
- **docker/** - Docker 容器監控與資料解析
- **i18n/** - 多語言支援與翻譯管理
- **ssh/** - SSH 連線管理與遠端命令執行
- **tui/** - Textual TUI 介面實作
- **utils/** - 配置管理、日誌系統等工具

## 開發指南

### 開發環境設置

```bash
# 1. 克隆專案
git clone <repository-url>
cd DockerMonitor

# 2. 建立虛擬環境
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或
venv\Scripts\activate     # Windows

# 3. 安裝開發依賴
pip install -r requirements.txt

# 4. 安裝為可編輯模式
pip install -e .
```

### 執行測試

```bash
# 執行所有測試
pytest

# 執行特定測試檔案
pytest tests/test_config.py

# 顯示詳細輸出
pytest -v

# 顯示測試覆蓋率
pytest --cov=src tests/

# 產生 HTML 覆蓋率報告
pytest --cov=src --cov-report=html tests/
```

### 程式碼格式化

```bash
# 使用 black 格式化程式碼
black src/

# 檢查但不修改
black --check src/

# 格式化特定檔案
black src/main.py
```

### 程式碼檢查

```bash
# 使用 flake8 檢查程式碼
flake8 src/

# 使用 mypy 進行型別檢查
mypy src/
```

### 使用 Makefile

```bash
# 安裝依賴
make install

# 執行測試
make test

# 程式碼格式化
make format

# 程式碼檢查
make lint

# 執行 TUI
make run

# 清理快取檔案
make clean
```

### 新增功能開發流程

1. **建立功能分支**
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. **開發功能**
   - 遵循現有程式碼風格
   - 添加必要的型別提示
   - 撰寫文件字串

3. **撰寫測試**
   ```python
   # tests/test_your_feature.py
   import pytest
   
   def test_your_feature():
       # 測試程式碼
       pass
   ```

4. **執行測試與檢查**
   ```bash
   pytest
   black src/
   flake8 src/
   mypy src/
   ```

5. **提交變更**
   ```bash
   git add .
   git commit -m "feat: add your feature description"
   ```

6. **推送並建立 Pull Request**
   ```bash
   git push origin feature/your-feature-name
   ```

### 程式碼規範

- 遵循 PEP 8 規範
- 使用 Black 進行程式碼格式化
- 所有函式都應有型別提示
- 所有公開 API 都應有文件字串
- 單元測試覆蓋率應 > 80%

## 疑難排解

### 連線問題

#### 無法連線至跳板機

**症狀**：
```
Error: Failed to connect to jump host
```

**解決方法**：

1. 檢查 SSH 金鑰權限
   ```bash
   # Linux/Mac
   chmod 600 ~/.ssh/id_rsa
   
   # Windows (PowerShell)
   icacls $env:USERPROFILE\.ssh\id_rsa /inheritance:r
   icacls $env:USERPROFILE\.ssh\id_rsa /grant:r "$($env:USERNAME):(R)"
   ```

2. 測試手動 SSH 連線
   ```bash
   ssh -i ~/.ssh/id_rsa user@jump.example.com
   ```

3. 檢查配置檔案
   - 確認 `hosts.yaml` 中的 hostname、username、key_file 正確
   - 確認金鑰檔案路徑存在

4. 啟用除錯模式查看詳細錯誤
   ```bash
   python -m src.main --debug status
   ```

#### 無法連線至目標主機

**症狀**：
```
Host: prod-web-01 - Status: Disconnected
```

**解決方法**：

1. 測試透過跳板機連線
   ```bash
   ssh -J user@jump.example.com user@target-host
   ```

2. 檢查跳板機是否可存取目標主機
   ```bash
   ssh user@jump.example.com "ping -c 3 target-host"
   ```

3. 確認目標主機 SSH 服務運行中

### Docker 問題

#### Docker 不可用

**症狀**：
```
Docker not available on host: prod-web-01
```

**解決方法**：

1. 檢查 Docker 是否安裝
   ```bash
   ssh -J user@jump.example.com user@target-host "docker --version"
   ```

2. 檢查使用者權限
   ```bash
   ssh -J user@jump.example.com user@target-host "docker ps"
   ```

3. 將使用者加入 docker 群組
   ```bash
   ssh -J user@jump.example.com user@target-host "sudo usermod -aG docker $USER"
   # 需要重新登入才會生效
   ```

4. 檢查 Docker 服務狀態
   ```bash
   ssh -J user@jump.example.com user@target-host "sudo systemctl status docker"
   ```

#### 無容器資料顯示

**症狀**：主機顯示已連線但沒有容器

**解決方法**：

1. 確認主機上確實有容器
   ```bash
   ssh -J user@jump.example.com user@target-host "docker ps -a"
   ```

2. 檢查 `hosts.yaml` 中該主機的 `enabled` 設定
   ```yaml
   enabled: true  # 確保為 true
   ```

3. 檢查是否被標籤篩選排除
   ```bash
   # 不使用標籤篩選
   python -m src.main tui
   ```

### 效能問題

#### TUI 反應緩慢

**解決方法**：

1. 減少並發連線數
   ```yaml
   # hosts.yaml
   monitoring:
     max_concurrent_connections: 3  # 降低並發數
   ```

2. 增加重新整理間隔
   ```bash
   python -m src.main tui --interval 120  # 2 分鐘
   ```

3. 減少監控的主機數量
   ```bash
   # 使用標籤篩選
   python -m src.main tui --tags production
   ```

#### 記憶體使用過高

**解決方法**：

1. 檢查是否有連線洩漏（查看日誌）
2. 重新啟動應用程式
3. 減少監控主機數量

### 配置問題

#### YAML 解析錯誤

**症狀**：
```
Error: Failed to load configuration
```

**解決方法**：

1. 檢查 YAML 語法
   ```bash
   # 使用線上 YAML 驗證器
   # 或使用 Python 檢查
   python -c "import yaml; yaml.safe_load(open('config/hosts.yaml'))"
   ```

2. 常見錯誤：
   - 縮排錯誤（必須使用空格，不能使用 Tab）
   - 冒號後缺少空格
   - 字串包含特殊字元未加引號

3. 使用範例檔案重新開始
   ```bash
   cp config/hosts.yaml.example config/hosts.yaml
   ```

### 日誌查看

#### 查看詳細日誌

```bash
# Windows
type logs\dockermonitor_*.log | more

# Linux/Mac
cat logs/dockermonitor_*.log | less

# 只看錯誤
# Windows
findstr "ERROR" logs\dockermonitor_*.log

# Linux/Mac
grep "ERROR" logs/dockermonitor_*.log
```

#### 啟用除錯日誌

```bash
# 啟用 DEBUG 等級日誌
python -m src.main --debug tui

# 日誌會寫入 logs/ 目錄
# TUI 模式下不會顯示在畫面上（避免干擾介面）
```

### 常見錯誤訊息

| 錯誤訊息 | 原因 | 解決方法 |
|---------|------|---------|
| `Permission denied (publickey)` | SSH 金鑰認證失敗 | 檢查金鑰路徑與權限 |
| `Connection timeout` | 網路連線逾時 | 檢查網路連線與防火牆設定 |
| `Docker not found` | Docker 未安裝或不在 PATH | 安裝 Docker 或設定正確路徑 |
| `Permission denied` (Docker) | 使用者無 Docker 權限 | 將使用者加入 docker 群組 |
| `No hosts configured` | 配置檔案為空或格式錯誤 | 檢查 hosts.yaml 配置 |

## 版本規劃

### 已完成功能 ✅

- [x] **v0.1.0** (2026-01-13) - 初始版本
  - SSH 隧道管理
  - 非同步多主機監控
  - Docker CLI 整合
  - TUI 互動介面
  - CLI 命令列介面
  - 多種輸出格式（Table/JSON/CSV）
  - 標籤篩選系統
  - 自動重新整理
  - 多語言支援（中文/英文）

- [x] **v0.1.1** (2026-01-14) - 問題修正
  - 修正 TUI 模式下日誌輸出干擾畫面問題
  - 修正 CPU 與記憶體統計資料未顯示問題
  - 修正重新整理後狀態訊息未清除問題
  - 優化離開時的日誌輸出

### 進行中功能 🚧

- [ ] **v0.2.0** (規劃中)
  - 容器搜尋與進階篩選功能
  - 容器日誌檢視器
  - 效能優化與連線池改進

### 未來規劃 📋

- [ ] **v0.3.0** - 容器管理功能
  - 容器啟動/停止/重啟
  - 容器刪除
  - 映像管理

- [ ] **v0.4.0** - 告警與通知
  - Email 通知
  - Slack 整合
  - 自訂告警規則
  - Webhook 支援

- [ ] **v0.5.0** - 歷史資料與分析
  - 容器運行時間追蹤
  - 資源使用趨勢
  - 效能圖表
  - 資料匯出與報表

- [ ] **v1.0.0** - 進階功能
  - 多跳板機支援
  - Web 儀表板（可選）
  - 外掛系統
  - 多使用者支援
  - 容器編排整合（Docker Compose/Swarm）

### 版本發布週期

- **修正版本**（0.x.y）：每 1-2 週，修正 bug 與小改進
- **次要版本**（0.x.0）：每 1-2 個月，新增功能
- **主要版本**（x.0.0）：重大功能更新或架構變更

## 授權與貢獻

### 授權條款

本專案採用 MIT 授權條款。詳見 [LICENSE](LICENSE) 檔案。

### 如何貢獻

我們歡迎各種形式的貢獻！

#### 回報問題

- 使用 GitHub Issues 回報 bug
- 提供詳細的重現步驟
- 附上錯誤訊息與日誌
- 說明您的環境（OS、Python 版本等）

#### 提交程式碼

1. Fork 本專案
2. 建立功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交變更 (`git commit -m 'Add some AmazingFeature'`)
4. 推送至分支 (`git push origin feature/AmazingFeature`)
5. 開啟 Pull Request

詳細貢獻指南請參考 [CONTRIBUTING.md](CONTRIBUTING.md)

#### 改進文件

- 修正錯字或不清楚的說明
- 新增使用範例
- 翻譯文件至其他語言

### 貢獻者

感謝所有貢獻者的付出！

### 聯絡方式

- **Issues**：https://github.com/yourusername/dockermonitor/issues
- **Discussions**：https://github.com/yourusername/dockermonitor/discussions

## 致謝

本專案使用了以下優秀的開源專案：

- [Textual](https://textual.textualize.io/) - 現代化 TUI 框架
- [AsyncSSH](https://asyncssh.readthedocs.io/) - 非同步 SSH 函式庫
- [Rich](https://rich.readthedocs.io/) - 終端格式化函式庫
- [Click](https://click.palletsprojects.com/) - CLI 框架
- [PyYAML](https://pyyaml.org/) - YAML 解析器

感謝所有開源社群的貢獻者！

---

## 快速連結

- 📖 [快速入門指南](QUICKSTART.md)
- 📝 [版本更新記錄](CHANGELOG.md)
- 🏗️ [技術架構文檔](CLAUDE.md)
- 🤝 [貢獻指南](CONTRIBUTING.md)
- 🌐 [語言切換說明](LANGUAGE_SWITCH.md)
- 🔗 [連線策略文檔](CONNECTION_STRATEGY.md)

---

**DockerMonitor** - 讓 Docker 容器監控變得簡單而優雅 🐳✨
